### in this task we calculating occurrences of each characters, `O(n)`

### sort occurrences `O(n)`

### we build an encoded Tree `O(n)`

### from encoded Tree we build encoded string `O(n)`

### so `O(n + n + n +n) => O(n)`
